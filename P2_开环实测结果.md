# P2 开环实测结果汇总

> 阶段:LEAD 基线 / P1(intent) / P2.1(条件化) / P2.2(条件化+碰撞代价)。
> 全部为**开环**(模型前向 + 指标,无 CARLA 闭环)。闭环 Driving Score 待 A800 上跑。
> 相关:方法 `方案_visual_intent_reactive_control.md`、`P2_control_head_实现方案.md`;对标 `EvaDrive_梳理与定位.md`。

---

## 0. 对比对象

| 模型 | 配置 | checkpoint |
| :-- | :-- | :-- |
| **LEAD** | posttrain,31ep,batch64(直接回归 waypoints) | `posttrain/model_0030` |
| **P1** | intent-only,31ep(只出 intent 场,不出 waypoints) | `intent_p1/model_0030` |
| **P2.1** | 条件化 control,8ep,batch128(欠训对照) | `control_p2/model_0007` |
| **P2.2** | 条件化 + 碰撞代价,31ep,batch128 | `control_p2_full/model_0030` |

---

## 1. 主表:开环一致评测(400 样本,同代码同数据,GPU)

> 用 `scripts/eval_collision_rate.py` 在同一批 400 样本上跑,跨模型可比。碰撞 = 预测 waypoint 落在 GT BEV 占据格(弱代理,见 §4)。

| 模型 | ADE↓ | FDE↓ | 轨迹碰撞率↓ | waypoint 碰撞率↓ |
| :-- | :--: | :--: | :--: | :--: |
| **LEAD** | 0.191 | 0.363 | 14.75% | 3.38% |
| **P2.1**(8ep,欠训) | 0.340 | 0.675 | 13.50% | 3.00% |
| **P2.2**(31ep) | **0.185** | **0.353** | 14.00% | 3.06% |

**读法**:
- **P2.2 的 ADE/FDE(0.185/0.353)≈ LEAD(0.191/0.363)**,甚至微微更好 → **条件化 + 碰撞代价不掉点,追平 LEAD**。
- **P2.1 差(0.340)= 只训 8ep 欠训**,不是方法问题(P2.2 训满就追平了)。
- **碰撞率三者接近**(14.0 vs 14.75 vs 13.5),差异在噪声内 → 碰撞代价**开环无可测收益**。

---

## 2. 训练期指标(wandb / tfevents,训练最后一步)

> 训练时的 running 指标,采样集与 §1 不同(故绝对值不同),仅作训练收敛参考。

| 模型 | waypoints_ade | waypoints_fde | route_ade | tsp_corr | loss_visual_intent |
| :-- | :--: | :--: | :--: | :--: | :--: |
| LEAD | 0.138 | 0.230 | 0.022 | 1.00 | — |
| P1 | — | — | — | — | 0.046 |
| P2.1(8ep) | 0.238 | 0.513 | 0.055 | 0.83 | 0.044 |
| P2.2 | 0.150 | 0.238 | 0.043 | 1.00 | **0.042** |

- **intent 头全程维持**:P1 0.046 → P2.2 0.042,加了 control 头也没拖坏(intent 喂 control 时 detach 的设计生效)。

---

## 3. 碰撞代价训练趋势(P2.2)

`unscaled_loss/loss_collision`:**首 0.117(step49)→ 末 0.087(step234589)**,降 ~26%。
→ 碰撞代价在被优化、轨迹学着避障,但**幅度小**(专家轨迹本身避障,留给它的空间有限)。

---

## 4. 诚实结论

**✅ 达成(机制层面)**
- **两层机制端到端跑通**:intent 场 → 条件化 control → waypoints,受碰撞代价约束。
- **质量追平 LEAD**:开环 ADE/FDE 与 LEAD 持平,不掉点。
- **intent 头健康**:收敛且维持,轨迹落在 intent 走廊内(见可视化)。

**⚠️ 未达成 / 局限(要正视,别自我误导)**
- **碰撞代价开环无收益**:轨迹碰撞率 14.0% vs LEAD 14.75%(噪声内),P2.1(无碰撞项)甚至 13.5%。原因:① 碰撞指标是弱代理(未来 waypoint 撞当前帧占据,障碍会动);② 专家本就避障、干净数据没留空间;③ **反应式避障的真正价值在闭环**(自车开偏后实时纠偏),开环测不出。
- **"追平 LEAD" ≠ "超过 LEAD"**:开环 imitation 匹配是"没坏",不是"更好"。
- **多模态未涌现**:intent 训到最后收敛成"专家单条路径的 soft 版"(单模),隐式多模需显式机制(地图 support / latent),尚未做。

**结论一句话**:P2 验证了**机制成立、不掉点**;方法的卖点(避障收益、多模)开环证不了,**必须闭环 Driving Score + 真多模机制**才能体现。

---

## 5. 可视化索引

| 阶段 | 内容 | 目录 |
| :-- | :-- | :-- |
| P0 | 专家轨迹→BEV intent 标签,叠 bev_semantic | `outputs/viz_visual_intent/` |
| P1 | intent 预测 vs GT(1/4/26 ep,看从糊到锐) | `outputs/viz_intent_pred{,_ep4,_ep27}/` |
| P2 | 预测 waypoints + intent 场 + 障碍 叠 BEV | `outputs/viz_trajectory/` |

生成脚本:`scripts/viz_visual_intent.py` / `viz_intent_pred.py` / `viz_trajectory.py` / `eval_collision_rate.py`。

---

## 6. 待闭环验证(A800）

- **LEAD vs P2.2 的 Bench2Drive / Longest6 Driving Score** —— 这才是方法有没有用的判据。
- 重点看:**碰撞/安全类子指标**(反应式避障是否在闭环体现)、以及分布外/纠偏场景 P2 是否比 LEAD 稳。
- 环境:A800(vulkaninfo 已确认可跑 CARLA),流程见 `闭环评测机_AutoDL_checklist.md`(A800 同理,普通用户免非-root workaround)。
